name: OCTO Background Pulse

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  pulse:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate background pulse
        run: |
          mkdir -p reports
          python3 - <<'PY2'
          import json, pathlib, time
          root = pathlib.Path.cwd()
          files = []
          for p in root.rglob("*"):
              if ".git" in p.parts:
                  continue
              if p.is_file():
                  files.append(str(p.relative_to(root)))
          payload = {
              "status": "manual_pulse_ok",
              "created_at": time.strftime("%Y-%m-%dT%H:%M:%SZ", time.gmtime()),
              "mode": "zero_cost_manual_report",
              "file_count": len(files),
              "offer": "OCTO Application Audit",
              "next_action": "Only run when a human explicitly needs a report or revenue task",
              "no_auto_submit": True,
              "scheduled_background_runs": False
          }
          pathlib.Path("reports/octo_background_pulse.json").write_text(json.dumps(payload, indent=2))
          PY2

      - name: Commit pulse report
        run: |
          git config user.name "octo-background-bot"
          git config user.email "octo-background-bot@users.noreply.github.com"
          git add reports/octo_background_pulse.json
          git commit -m "chore: update manual octo pulse" || echo "No changes to commit"
          git push || echo "Push skipped"
