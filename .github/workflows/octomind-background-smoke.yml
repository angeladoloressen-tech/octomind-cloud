name: Octomind Background Smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Inspect project
        run: |
          echo "octomind_background_smoke=manual"
          pwd
          find . -maxdepth 2 -type f | sort | head -120

      - name: Node smoke
        run: |
          if [ -f package.json ]; then
            npm ci || npm install
            npm run build --if-present
            npm test --if-present
          else
            echo "package_json_not_found"
          fi

      - name: Python smoke
        run: |
          python --version
          if [ -f requirements.txt ]; then
            python -m pip install -r requirements.txt
          fi
          python - <<'PYSMOKE'
          import json
          from pathlib import Path
          result = {
              "status": "ok",
              "mode": "zero_cost_manual_smoke",
              "has_package_json": Path("package.json").exists(),
              "has_requirements": Path("requirements.txt").exists(),
              "has_src": Path("src").exists(),
              "scheduled_background_runs": False,
          }
          print(json.dumps(result, indent=2))
          PYSMOKE
